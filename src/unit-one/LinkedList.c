struct Node {
    int data;
    struct Node *next;
};

struct LinkedList {
    int length;
    struct Node *head;
    struct Node *tail;
};

int LinkedList(){
    struct LinkedList linkedList;

    return 0;
}

